version: 0.0
Resources:
  - TargetService:
      Type: AWS::ECS::Service
      Properties:
        TaskDefinition: rendered-taskdef.json
        LoadBalancerInfo:
          ContainerName: jobtory-backend
          ContainerPort: 8000
